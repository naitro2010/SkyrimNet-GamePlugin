{% if render_mode == "full" or render_mode == "thoughts" or render_mode == "transform" or render_mode == "dialogue_target" %}
{{ "## Physical Activity" }}
{% if is_unconscious(actorUUID) %}
- {{ decnpc(npc.UUID).first_name }} is currently unconscious and unable to move or act.
{% endif %}
{% if is_sneaking(actorUUID) %}
- {{ decnpc(npc.UUID).first_name }} is currently trying to move silently, and avoid being seen or heard.
{% endif %}
{% if is_walking(actorUUID) %}
- {{ decnpc(npc.UUID).first_name }} is currently walking at a steady pace.
{% endif %}
{% if is_sprinting(actorUUID) %}
- {{ decnpc(npc.UUID).first_name }} is currently sprinting at full speed.
{% endif %}
{% if is_swimming(actorUUID) %}
- {{ decnpc(npc.UUID).first_name }} is currently swimming.
{% endif %}
{% if is_knocked_down(actorUUID) %}
- {{ decnpc(npc.UUID).first_name }} is currently badly wounded, and is kneeling on the ground.
{% endif %}
{% endif %}
